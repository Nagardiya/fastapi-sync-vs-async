# fastapi-sync-vs-async
"This repository demonstrates the difference between synchronous and asynchronous APIs using FastAPI. It contains simple examples showing how sync APIs block requests while async APIs handle multiple requests concurrently."
